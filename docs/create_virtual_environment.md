# Creating Virtual Environment

```zsh
$ python3 -m venv venv
```

```zsh
$ source venv/bin/activate
```

```zsh
(venv) $ pip3 freeze
```

```zsh
(venv) $ pip3 list
```

```zsh
(venv) $ pip3 freeze > requirements.txt
```